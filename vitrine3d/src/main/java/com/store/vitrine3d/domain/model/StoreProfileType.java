package com.store.vitrine3d.domain.model;

public enum StoreProfileType {
    /** Loja padrão: contato via WhatsApp. */
    STANDARD,
    /** Vitrine de afiliado: cada produto tem um link externo de indicação. */
    AFFILIATE
}
